import { CasteFilterPipe } from './caste-filter.pipe';

describe('CasteFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CasteFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
